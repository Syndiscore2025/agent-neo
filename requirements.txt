# Core FastAPI dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Database (PostgreSQL enforcement)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
alembic>=1.13.0

# LLM/AI integrations (for interactive layer)
anthropic>=0.18.0
openai>=1.12.0

# Optional: local semantic embeddings for RepoIndex (Phase 2 live index).
# Heavy (pulls in torch); the index degrades gracefully to keyword search when
# absent, so this is intentionally left OUT of the default deploy footprint.
# Enable locally with:  pip install sentence-transformers
# sentence-transformers>=2.7.0

# Image processing (for attachments)
pillow>=10.2.0

# PDF processing (for attachments)
pypdf>=4.0.0
pdfplumber>=0.10.0

